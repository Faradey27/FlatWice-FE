import theme from './../../assets/theme';

export default `
  .flat-card {
    display: flex;
    flex-wrap: wrap;
    height: 535px;
    max-width: 420px;
    width: 100%;
    box-shadow: ${theme.cardBoxShadow};
  }
`;
