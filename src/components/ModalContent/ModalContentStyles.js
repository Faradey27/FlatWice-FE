import theme from './../../assets/theme';

export default `
  .modalContent {
    display: flex;
    flex-wrap: wrap;
    width: 540px;
    height: 370px;
    padding: 30px;
    background-color: ${theme.white};
    box-shadow: ${theme.boxShadow};
  }
`;
