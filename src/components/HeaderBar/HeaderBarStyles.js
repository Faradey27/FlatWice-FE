import theme from './../../assets/theme';

export default `
  .headerBar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 60px;
    padding: 0 10px;
    box-shadow: ${theme.boxShadow};
  }
`;
